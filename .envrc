# Databricks configuration
export DATABRICKS_CONFIG_PROFILE=logfood

# Load .env file if it exists (for local secrets)
dotenv_if_exists .env

# Lakebase connection (set these in .env)
# export LAKEBASE_HOST=
# export LAKEBASE_DATABASE=
# export LAKEBASE_USER=
# export LAKEBASE_PASSWORD=

# Electric SQL (set in .env)
# export ELECTRIC_URL=
