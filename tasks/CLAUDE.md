# Session Recovery Instructions

**Read `tasks/checkpoint.md` first** - it contains the current state of work.

## Quick Context
- Building SSA Activity Dashboard with data sync infrastructure
- Three-tier architecture: Logfood → Delta → Lakebase → App
- Just completed: sync jobs, test suite, architecture docs with Mermaid

## Immediate TODO
1. Commit pending changes with jj
2. Create PR to main
3. See checkpoint.md for exact commands

## Shell Notes
- Fish shell only
- Use `jj` not `git`
- Use `isaac` not `vibe` for CLI commands
